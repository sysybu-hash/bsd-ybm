/**
 * BSD-YBM — default global “Golden Helix” mark (`public/image_8.png` | `public/image_8.svg`).
 * Both paths are under `/public`; Next serves them as `/image_8.png` and `/image_8.svg`.
 */
export const GOLDEN_HELIX_LOGO_PNG = '/image_8.png';
export const GOLDEN_HELIX_LOGO_SVG = '/image_8.svg';

/** Default to SVG — `image_8.png` may be absent; use PNG when you add it to `/public`. */
export const DEFAULT_GLOBAL_BRAND_LOGO = GOLDEN_HELIX_LOGO_SVG;
